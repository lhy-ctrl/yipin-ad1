# 易品广告记账开单系统 - 云端部署指南

## 一、Supabase 配置（必须先完成）

### 1. 创建项目
1. 登录 https://supabase.com
2. 点击 "New Project"
3. 填写项目名称（如 yipin-ad），设置数据库密码，选择区域（选离你近的，如 Singapore）
4. 等待项目创建完成（约2分钟）

### 2. 执行建表脚本
1. 进入项目后台 → 左侧菜单 "SQL Editor"
2. 点击 "New query"
3. 打开本项目根目录的 `supabase_schema.sql`，复制全部内容粘贴进去
4. 点击 "Run" 执行
5. 看到 "Success. No rows returned" 表示成功

### 3. 创建登录用户
1. 左侧菜单 "Authentication" → "Users"
2. 点击 "Add user" → "Create new user"
3. 填写你的邮箱和密码（这就是你登录系统的账号）
4. 点击 "Create user"

### 4. 获取配置信息
1. 左侧菜单 "Project Settings" → "API"
2. 复制 "Project URL"（如 https://xxxx.supabase.co）
3. 复制 "anon public" key（以 eyJ 开头的长字符串）

### 5. 填入应用配置
打开 `js/supabase.js`，修改前5行：
```javascript
var SUPABASE_URL = '你的Project URL';
var SUPABASE_ANON_KEY = '你的anon key';
var SUPABASE_EMAIL = '你在Supabase创建的用户邮箱';
var SUPABASE_PASSWORD = '你在Supabase创建的用户密码';
var ACCESS_PASSWORD = 'Yan941207.'; // 网页访问密码，可修改
```

**登录方式**：打开网页只需输入访问密码 `Yan941207.`，系统会自动用预设的Supabase账号登录云端，无需输入邮箱。

## 二、GitHub Pages 部署

### 方法一：通过 GitHub 网页上传（简单）
1. 登录 https://github.com，创建新仓库（如 yipin-ad），设为 Public
2. 点击 "uploading an existing file"，把项目所有文件拖进去（包括 index.html、css/、js/、assets/）
3. 提交后，进入仓库 Settings → Pages
4. Source 选 "Deploy from a branch"，Branch 选 main，文件夹选 /(root)
5. 点击 Save，等待1-2分钟，页面会显示访问地址（如 https://你的用户名.github.io/yipin-ad/）

### 方法二：通过 Git 命令行（推荐）
```bash
cd /Users/ylh/Documents/记账
git init
git add .
git commit -m "初始版本"
git branch -M main
git remote add origin https://github.com/你的用户名/yipin-ad.git
git push -u origin main
```
然后在 GitHub 仓库 Settings → Pages 中开启 Pages。

## 三、使用说明

1. 打开部署后的网址，输入你在 Supabase 创建的邮箱密码登录
2. 数据自动存储在云端，换设备登录数据同步
3. 每次操作后自动同步到云端
4. 侧边栏底部有退出登录按钮

## 四、数据安全

- 所有表启用了行级安全（RLS），只有登录用户能访问数据
- anon key 是公开的，但没有登录token无法读写数据
- 建议定期在 "数据备份" 页面导出 Excel 备份

## 五、本地开发

直接双击 index.html 也可以使用，但需要先配置好 Supabase 信息并联网。
